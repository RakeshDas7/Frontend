// console.log(location.reload())
// location.href = "index.html"

// history represents how many pages are there availble in the browser

// history.go()
// history.back()
// history.go(-1) = history.back()

// innerWidth
// innerHeight

// outerHeight
// outerWidth

// open("link")

// close() -> // VM190:1 Scripts may close only the windows that were opened by them. all tabs cann't be close by this method 